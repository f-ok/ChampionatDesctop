<Window x:Class="подготовка.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:подготовка"
        mc:Ignorable="d"
        Title="Авторизация" Height="200" Width="250"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        WindowStartupLocation="CenterScreen"
         WindowStyle="None"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Text="Введите id пользователя" x:Name="nameOfUser"/>
        <TextBox Grid.Row="1" Text="Введите пароль" x:Name="passwordOfUser"/>
        <Button Grid.Row="2" Content="Вход" Click="Button_Click" />
        
    </Grid>
</Window>
